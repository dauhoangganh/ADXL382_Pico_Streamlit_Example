if sample_count == 10:
                        for _ in range(10):
                            samples.popleft()
                            x_vals.popleft()
                            y_vals.popleft()
                            z_vals.popleft()